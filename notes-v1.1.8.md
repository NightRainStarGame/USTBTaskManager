# TaskManager v1.1.8 更新说明

> 建议 v1.1.6 / v1.1.7 用户通过「设置 → 软件更新」增量升级（补丁仅十几 MB，无需重下整包）。首版安装请走 Setup 包。

## 更新体验

- **补丁更新全自动收尾**：NSIS 应用内静默模式（`/S` 完全静默化）——点一次「立即安装并重启」就完事，等一会自己重新打开新版本，不再弹「为我安装 / 选目录」向导。
- 双击 Setup 仍走原完整向导流程（首次安装可手动选目录），体验不变。

## 作业同步

- **一个同步码可以发多条作业**：发布作业时点 `+ 添加作业条目` 可添加多条（每条独立标题 / 内容 / 截止 / 类型），一次性发布。同学拿到同一个同步码就能一次接收所有条目。
- 后端批量发布单次远端 IO（一次 GitHub PUT / 一次云盘上传），不增加请求次数。
- **多平行班接收挂载计数精确化**：之前只显示第一个课程的条数，现在每个课程独立显示真实 created / updated 条数（按 `remote_id` 加 `#c<courseId>` 已保证精度）。
- `mergeEntry` 按 `(sessionDate, title)` 去重保留覆盖语义，批量条目不同标题不会互相覆盖。

## 范式

- `PublishPayload` 新增 `entries?: PublishEntry[]`；批量与单条走同一接口，后端兼容。
- `SyncResult` 新增 `perCourse[]`，前端 UI 按课程分行展示。