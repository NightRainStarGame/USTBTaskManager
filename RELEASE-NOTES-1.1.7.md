# TaskManager v1.1.7 更新说明

> 建议所有 v1.1.6 用户通过「设置 → 软件更新」增量升级（补丁约十几 MB，无需重下整包）。

## 作业同步

- **课程通用固定 ID（courseKey）**：同名同授课老师的课程在所有设备上派生同一个 ID（CK- 前缀）。
  作业发布/接收凭它精确挂载，彻底解决「作业挂错课程 / 只同步到最开始一条」的问题。
- 同名同老师的课程（含平行班）**共用同一个同步作业码与作业包**；接收端命中的所有平行班一起挂载。
- **北科云盘目录转接范式**：`homework/<作业发布码>/<课程ID>-<时间戳>.json`——发布码建文件夹、
  课程 ID 命名文件，同步对应一目了然。旧扁平结构自动兼容迁移。

## 版本更新 / 补丁

- 统一文件命名范式：`TaskManager-Patch-<from>-to-<to>.zip`，清单 latest.json 指明哪个版本该更新到哪。
- 北科云盘源现在也分发增量补丁（补丁下载支持云盘文件名解析，不再强依赖 GitHub 直链）。

## 界面

- **左上角 Logo 换新**：StarMain server-icon。
- **樱花粉主题大改**：柔光玻璃（frosted glass）面板、背景更粉、文字对比度大幅提升、
  粒子背景从几何体换成**漂浮爱心**。

## 修复

- 输入框偶发「点不上/没焦点」：新增点击补焦兜底修复（焦点丢失自动恢复），并记录修复日志供诊断。
- 粒子背景限帧 30fps，降低 GPU 占用（低配机器输入卡顿的缓解项）。
