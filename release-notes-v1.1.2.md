## v1.1.2 — 课表数据安全加固 + 作业码制重构

### 课表数据安全（重要修复）
- **xls / 教务导入前自动安全备份**：任何覆盖式导入前，先在 `backups/` 里落一份 `pre-import-*.db`，误操作可随时找回
- 自动滚动备份保留份数 7 → 14 份
- 修复一个数据丢失隐患：此前教务导入的 `replaceExisting` 会连带删除上次导入的课程；现在导入前有安全备份兜底

### 作业同步（码制 v2.1）
- 「作业同步」菜单分三个入口：
  - **生成作业码**：一键生成码对（作业发布码 = 密钥 12 位，同步作业码 = 分享码 8 位），高亮展示 + 一键复制
  - **发布作业**：只需输入作业发布码即可验证进入发布界面（添加作业、关联课程、上传）
  - **接收作业**：输入同步作业码，作业自动挂到对应课程（没有的课程自动创建）
- 发布码改为自包含格式（前 8 位即同步码 + 4 位校验），网站端按 `docs/HOMEWORK-CODES.md` v2.1 规范实现即可互通

### 其他
- 多源更新架构（GitHub leastversion + 云盘源占位），启动时自动检查并弹窗提醒
- NSIS 安装器自动清空旧版运行时缓存，杜绝新旧版本代码堆叠
