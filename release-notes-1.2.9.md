# v1.2.9 — 班级模块全面重做 + 开箱即用

> 一句话：**v1.2.9 把班级模块从根上重做——P2P 同步四大根因修复、接龙/投票/成员管理全新上线、零配置开箱即用**。

---

## 🎉 最重要的变化：开箱即用

**新用户装完就能用班级功能，什么都不用配。**

- 内置公共写入通道已启用（详见下方 R9）
- 创建班级、发公告、接龙、投票、成员同步全部直接工作
- 不再需要「先去申请 GitHub 令牌」这道门槛

---

## 🏗️ 数据通道重构（R9）

### 独立数据仓库
- 班级数据搬到独立仓库 **USTBTaskManager-Class**（与主程序更新清单完全隔离）
- 读：raw CDN 直连（cache-bust 防缓存脏读）
- 写：GitHub Contents API（PUT sha + 409 冲突重试）

### 令牌三级策略
1. **个人 PAT**（可选）：独立配额，更快
2. **内置公共令牌**（默认）：个人令牌缺失/失效/限流（401/403/429）时自动降级
3. 通道状态三态显示：✅ 个人 / ✅ 内置 / ⚠️ 不可用

### 安全边界（已实测验证）
- 内置令牌是 fine-grained PAT：**只能**读写班级数据仓库
- 对主仓库写入返回 403——即使泄露，更新机制不受影响，班级数据可 git 回滚
- 云盘（AnyShare）降级为可选备源，默认关闭；修复了配置后反而不读云端的失联 bug

---

## 🐛 P2P 同步四大根因修复（R1）

| # | 根因 | 修复 |
|---|---|---|
| 1 | sync 时序错乱（先读后写顺序导致覆盖新数据） | 重写 syncCore：严格 fetch → merge → publish 顺序 |
| 2 | 依赖 GitHub dir API 列文件（会漏、会限流） | manifest 显式索引（不参与签名） |
| 3 | 文件名隐式生成（同名校验碰撞） | 显式 `id` + `author_alias` 命名 |
| 4 | join 不写云端（成员列表只存在本地） | join 时同步写云端 members |

另有：公告删除走 **tombstone**（软删除标记，防止删除被旧节点复活）；后台轮询修复 `last_synced_at` 字段名错误（此前每 30s 静默抛错）。

---

## 🆕 新功能

### 成员管理（R3）
- 班主任/管理员可 **调整角色**（admin / member）
- **移除成员**（踢人）+ 被移除方自动检测（下次同步发现 `member_synced` 失效，班级自动消失并提示）

### 接龙（R4）
- 发布接龙（截止时间、名额上限可选）
- 参加接龙 / 结束接龙
- **多写冲突合并**：两个人同时报名最后一条——fetch → merge → PUT，409 自动重试，不再互相覆盖

### 投票（R5）
- 发起投票（多选项、单/多选、截止时间）
- 投票 / 结束投票
- 同样带多写冲突合并（两人同时投不同选项都能保留）

### 公告增强（R2）
- 公告支持删除（tombstone）
- 原「作业发布」合并进公告（**作业 tab 已移除**，PublishTaskModal 退役）

---

## 🎨 UI 重构（R6/R7）

### ClassDetail 五 Tab
1. **总览**：班级统计（人数、公告数、进行中接龙/投票）
2. **公告**：列表 + 发布 + 删除
3. **接龙**：进行中/已结束分组
4. **投票**：实时票数 + 进度条
5. **成员**：角色管理 + 移除

### Settings
- 月卡订阅区 **锚点滚动 + 高亮**，从更新弹窗点「查看月卡」直达

---

## 📊 改动量

| 维度 | 数据 |
|---|---|
| 重写模块 | electron/class/（index.ts 核心重写 + storage.ts 通道重构） |
| 新增 IPC | deleteAnnouncement / promoteMember / removeMember / 接龙 ×4 / 投票 ×4 |
| UI 重构 | ClassDetail 5 tab（删作业 tab + PublishTaskModal） |
| 数据仓库 | 新建 USTBTaskManager-Class（独立、隔离、可回滚） |

---

## ⚠️ 升级注意

- **DB 自动迁移**：新增表/列（接龙、投票、tombstone、author_alias、member_synced）首次启动自动完成
- **旧班级数据兼容**：v1.2.8 及以前的本地班级在首次同步后自动迁移到新索引结构
- **作业功能**：并入公告 tab；原有作业数据以公告形式保留
- 作业 tab / PublishTaskModal 移除不影响主程序其他模块

---

## 🔮 v1.3.0 路线（草案）

- 班级图片附件 UI（API 已就位）
- 班主任禁言 / 班级治理增强
- store 拆 slice（settingsSlice / dataSlice / uiSlice + useShallow 优化）
- 接龙/投票的更新推送（本地通知）
