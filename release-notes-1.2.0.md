# TaskManager v1.2.0 更新日志

## 🐛 修复：同步作业出现两条重复
- 根因：同一条作业在 GitHub 和北科云盘各生成过一个不同 ID，接收合并时两条都保留
- 现在同课程+同日期+同标题的条目只保留最新一份，两个源合并后绝不重复
- **自动愈合**：本地已有的历史重复条目，下次接收时自动合并为一条并提示
  「已自动合并 N 条重复作业」
- 远端已损坏的码包在下次发布时自动收敛修复
- remote_id 新旧格式兼容，重复接收幂等不重复插入
