# v1.2.4 更新说明

> 本次为 v1.2.3 之后的修复补丁，针对桌面启动体验。

## 🐛 启动体验修复

### Splash 启动动画「立刻可见」

之前点击应用图标后存在一段黑屏/静止窗口，**等待主进程跑完 DB 初始化 + IPC 注册 + 主窗口构造**之后才看到启动动画。修复后 splash 窗口在 `whenReady` 后**立即显示**，操作系统在窗口 background 时先把首帧画好（`paintWhenInitiallyHidden: true`），配合黑色背景，视觉上与主进程初始化重叠，用户**点图标即看到动画**，无明显黑屏。

技术细节：移除了对 `ready-to-show` 事件的依赖（Electron 在同步阻塞期间不触发该事件）。保险丝保留在背景运行期，不再承担主路径职责。

## 🛠 升级说明

- 已装 v1.2.3 的用户：自动应用增量补丁 ~22MB，无需重装
- 新装用户：下载完整安装包 ~97MB
- 数据无损保留，无需迁移