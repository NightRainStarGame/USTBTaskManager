feat(core): v1.2.10 更新链路修复、云端规范化与移动端能力落地

update:
- 补丁下载支持多源镜像回退，主源 404 不再直接硬失败
- patch-helper 把终态(applied/failed/sidecar)与失败原因写回状态文件，
  写完 .new 旁路后补 relaunch，消除「下完却永远装不上」的静默失败
- 新增 baseline-mismatch / asar-unreadable 归因，全源失败返回 fallback:full 引导走整装

perf:
- 主进程加单实例锁，避免双实例重复初始化 DB、抢注册全局快捷键
- 路由级按需加载，首包剥离 Settings/Projects/Courses/ClassDetail
- 更新检查延后 6s，不与首屏抢资源；统一动效令牌并修复未定义的 animate-in

cloud:
- 作业同步数据迁至 USTBTaskManager-Class/homework/，与班级共用同一张
  fine-grained PAT，内置令牌兜底实现开箱即用
- 主仓库同路径历史包只读兜底，迁移前分发的老同步码继续可用
- 实测该 PAT 对数据仓 homework/ 写入 201、删除 200

mobile:
- 放开作业同步入口(handler 本就注册且 GitHub 支持 CORS，此前判断有误)
- 新增零依赖 Android 原生 HTTP 桥(CapacitorHttp 可用则用，否则回退 fetch)，
  Set-Cookie 经自定义头透传，保证原生层的 Cookie 串联不丢

refactor:
- 移除小程序模块：2 个文件 / 28 处接线，含 5 处 SQL 级联删除
